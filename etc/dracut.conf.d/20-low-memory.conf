# Keep initramfs generation within the memory budget of small LBaaS guests.
# Older dracut releases invoke the custom command directly, while newer
# releases map it to 3cpio and read the level from compress_level_zstd.
compress="zstd -1 -q -T1"
compress_level_zstd="1"
